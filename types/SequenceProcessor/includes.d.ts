/**
 * @param {AsyncIterable<Iterable<[Path, Value, number, number]>>} asyncIterable
 * @param {string} includes
 * @returns {AsyncIterable<Iterable<[Path, Value, number, number]>>}
 */
export default function includes(asyncIterable: AsyncIterable<Iterable<[Path, Value, number, number]>>, includes: string): AsyncIterable<Iterable<[Path, Value, number, number]>>;
import { Path } from "../lib/path.js";
import { Value } from "../lib/value.js";
//# sourceMappingURL=includes.d.ts.map